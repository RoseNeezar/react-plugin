import React from "react";
import ReactDom from "react-dom";

const Options = <img src="icon.png" />;

const root = document.createElement("div");
document.body.appendChild(root);

ReactDom.render(Options, root);
